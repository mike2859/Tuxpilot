using CommunityToolkit.Mvvm.ComponentModel;
using Tuxpilot.UI.Models;

namespace Tuxpilot.UI.ViewModels;


/// <summary>
/// ViewModel pour un message du chat
/// </summary>
public partial class ChatMessageViewModel : ObservableObject
{
    [ObservableProperty]
    private string _texte = string.Empty;
    
    [ObservableProperty]
    private bool _isUser;
    
    [ObservableProperty]
    private string _icone = "🤖";
    
    [ObservableProperty]
    private string _backgroundColor = "BackgroundSecondary";
    
    [ObservableProperty]
    private string _textColor = "TextPrimary";
    
    [ObservableProperty]
    private bool _hasAction;
    
    [ObservableProperty]
    private AssistantAction? _action;
    
    [ObservableProperty]
    private bool _actionExecuted;
    
    [ObservableProperty]
    private string _actionResult = string.Empty;
    public ChatMessageViewModel(string texte, bool isUser)
    {
        Texte = texte;
        IsUser = isUser;
        
        if (isUser)
        {
            Icone = "👤";
            BackgroundColor = "BackgroundInfo";
            TextColor = "TextPrimary";
        }
        else
        {
            Icone = "🤖";
            BackgroundColor = "BackgroundSecondary";
            TextColor = "TextSecondary";
        }
    }
}